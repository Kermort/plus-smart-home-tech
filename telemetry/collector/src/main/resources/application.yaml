spring:
  application:
    name: collector
  config:
    import: configserver:http://localhost:8887

  cloud:
    config:
      fail-fast: true
      retry:
        useRandomPolicy: true
        max-interval: 6000